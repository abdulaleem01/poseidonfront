import { DigitToValuePipe } from './digit-to-value.pipe';

describe('DigitToValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DigitToValuePipe();
    expect(pipe).toBeTruthy();
  });
});
